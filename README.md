# SP500-Constituents-Tracker
Allows you to find the constituents of the SP500 at a specific date. This can be used to eliminate survivorship bias in backtesting. 
This also works for any universe with a column for "date" and a column for "tickers".
The code will let you define the names for each column if they are named differently.
